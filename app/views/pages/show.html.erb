<% title @page.title %>
<% meta_keywords @page.meta_keywords || @page.title %>
<% meta_description @page.meta_description || @page.title %>

<div class="row">
  <div class="span2 text-center" style="background: #eee;">
      <h4>
        <%= @page.title %>
      </h4>
  </div>
  <div class="span10">
    <div class="content">
      <%= @page.body.html_safe %>
    </div>
  </div>
</div>
