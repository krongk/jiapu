<%= simple_form_for(@jiapu) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :owner_id %>
    <%= f.input :name %>
    <%= f.input :note %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
